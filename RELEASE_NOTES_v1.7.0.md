## TTDesktop1.0 v1.7.0 更新说明

### API 设置（重要）
- 新增程序内 **「API 设置」** 弹窗，无需用记事本改 JSON
- 支持填写 authorization、username、环境 prod/test、工单组 rgIds
- **工单组改为逐行填写**：点「+ 添加工单组」一行一个 RG ID，可删除
- 菜单 **文件 → API 设置…** 也可打开；保留「打开配置文件」高级入口
- 保存后自动写入合法 UTF-8 JSON，下次拉单即生效

### 来单改标题
- 修复 API 发现新单后卡住、不刷新 TT 列表：改标题与 F5 不再互相阻塞
- 列表未出现工单时自动刷新 TT 页面，再重试改标题
- 英文工单：自动剔除 Store/ID&Name 中误填的 `Station` 前缀

### 工单列表
- API 模式下合并 TT 页面 DOM，减少「TT 有 7 单、程序只显示 1 单」
- API 少单时放宽处理人条件并提示补齐

### 故障识别提示
- **高频问题（单站反复）** 与 **短时批量（多站同类）** 日志、徽章、通知文案区分更清晰
- 顶栏：`高频单站` / `短时批量`；日志含「类型」说明，避免误读为同一功能

### 使用提示
- 正式环境请选择 **prod**；username 与界面「处理人 MIS」保持一致
- 工单组 rgIds 需覆盖本人会接到的所有 RG；漏配会导致 API 少单
