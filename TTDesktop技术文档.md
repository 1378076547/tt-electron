# TTDesktop 技术说明

> 版本：v1.8.2  
> 形态：Windows 桌面客户端（Electron）  
> 用途：给领导或交接时说明「程序跑在哪、连谁、不自建什么」

操作步骤见《TTDesktop使用手册》。本文只讲架构与边界。

---

## 1. 一句话

TTDesktop 是装在接单员电脑上的**本地客户端**。它没有自己的业务服务器。工单数据来自美团 TT；页面上的接单、建群、改标题，是在内嵌的 TT 网页里完成的。

不是传统「我们自己写一套 Server + 数据库，客户端只负责展示」的 C/S。

---

## 2. 架构

```
接单员电脑
└─ TTDesktop（Electron）
   ├─ 主进程：窗口、托盘、日志落盘、调 TT 查询 API、检查更新
   └─ 界面
      ├─ 左侧：日志 / 工单列表 / 话术
      └─ 右侧：内嵌 TT 网页（登录态在本机）
           └─ 接单、建群、发话术、改标题、拉 PM：在这个网页上操作

外部系统（都不是本程序自建的）
├─ TT 网页    tt.sankuai.com          登录、处理工单
├─ TT 查询 API ticket.vip.sankuai.com  拉工单列表等只读数据
└─ S3         s3plus 静态目录          安装包更新、PM 对照表
```

| 问题 | 答案 |
|---|---|
| 有没有我们自己的业务服务器？ | **没有。** 不部署、不运维业务后端。 |
| 工单存在哪？ | 存在 TT。本程序只在内存里缓存当前列表，关掉就没了。 |
| 配置存在哪？ | 本机：开关在浏览器本地存储；API 令牌、高频/短时配置、日志在用户数据目录。 |
| S3 干什么？ | 只放**安装包**（自动更新）和 **PM.csv**（按地区拉人用的对照表）。不跑业务逻辑。 |
| 别人的电脑要不要连我的电脑？ | 不要。每人自己装、自己登录 TT。 |

Server 端**不需要**为本工具单独开发。要用到的外部能力只有：

1. **TT 网页**：账号能登录、能在页面上处理工单。  
2. **TT 查询 API**：本机配置里填 MIS 和 authorization，用来补齐工单列表（查不到时仍可从页面读）。  
3. **S3 目录**（可选）：要做自动更新时，把安装包放到约定地址；要拉 PM 时，把 `PM.csv` 放到 `Synchronize/`。

---

## 3. 两条通道

多数能力是「API 看数据 + 网页上动手」，不是纯 API。

| 通道 | 做什么 | 不做什么 |
|---|---|---|
| TT 查询 API | 工单列表、待处理、高频/短时扫描用的数据 | 不接单、不建群、不改标题 |
| 内嵌 TT 网页 | 开始处理、建群、发话术、改标题、改优先级、拉 PM、标题染色 | 依赖 TT 页面结构，TT 改版时要跟着改 |

右侧列表标题变色（24h 橙 / 48h 红、高频与短时标红）是往 TT 页面里加样式，**不改 TT 服务器上的数据**，刷新或告警消失后颜色会按当前状态重画。

---

## 4. 程序里有什么（v1.8.2）

| 模块 | 作用 |
|---|---|
| 自动接单 | 定时在 TT 页点「开始处理」。总开关默认关。 |
| 建群 / 话术 | 接单后可选建大象群、按标题关键词发话术。 |
| 来单改标题 | 按发起人架构拼「事业部 + 城市 + 站点 + 原问题」。无架构不改。 |
| 工单列表 | API 与页面合并展示；可筛「仅看我的」、隐藏已关闭。 |
| 优先级 | 可批量或自动把单往上升（默认关）。 |
| 48h 时效 | 左侧历时提醒；右侧标题约 24h 橙、48h 红。提醒默认开，系统弹窗默认关。 |
| 高频单站 / 短时批量 | 扫当前相关工单，左侧告警；关联标题在 TT 列表标红。 |
| 拉 PM | 用本机（或从 S3 同步的）PM 表，在已有群里加对应地区的人。 |
| 电表异常 | 标题含【电表异常】且 Retail / it_retail：仍接单，不建群、不发话术。 |
| 多标签 | 第一个标签固定接单；其它标签可浏览任意网址，不打断后台接单。 |
| 自动更新 | 启动时看 S3 上有没有新安装包，有则询问下载。 |

---

## 5. 默认开关（第一次打开、还没改过设置）

不是全部打开。

| 开关 | 默认 |
|---|---|
| 自动接单（开始） | 关 |
| 自动建群 | 关 |
| 自动发话术 | **开** |
| 来单改标题 | 关 |
| 标题巡检（只打日志） | 关 |
| 自动升高优先级 | 关 |
| 48h 时效提醒 | **开** |
| 48h 的 Windows 通知 | 关 |
| 仅看我的 / 隐藏已关闭 | 开 |
| 高频、短时扫描 | 开（没配关闭文件时）；是否出告警还取决于有没有配 API、有没有达到阈值 |

改过的开关记在本机，下次打开沿用。

---

## 6. 本机文件（不进我们的服务器）

安装后大致在：

`C:\Users\<用户名>\AppData\Roaming\TTDesktop1.0\`

| 内容 | 文件 |
|---|---|
| API 令牌、MIS、工单组 | `tt-api.local.json`（含密钥，不要外传） |
| 高频 / 短时规则 | `hf-issue.local.json`、`burst-outbreak.local.json` |
| 按天日志 | `logs\YYYY-MM-DD.txt` |
| 界面开关、话术规则 | 程序本地存储，不单独成业务库 |

---

## 7. 明确不做的事

- 不自建工单库，不把工单同步到自有服务器。  
- 不提供给组长看全组的管理后台（当前是接单员自己的工具）。  
- 不在 TT 页面上改布局或删官方按钮；标题变色只是叠加颜色。  
- 不支持 Mac 安装包（当前只打 Windows）。  
- 关单、评论、转单、催办尚未做成自动流程。
