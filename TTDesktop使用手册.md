# TTDesktop1.0 使用手册

> 版本：v1.8.2  
> 适用：Windows 安装版（`TTDesktop1.0-Setup-x.x.x.exe`）  
> 用途：自动接单、改标题、发话术、拉 PM、优先级辅助、故障提醒

---

## 1. 它是做什么的

TTDesktop 是内嵌 TT 网页的桌面工具。右侧是 TT 页面，左侧是日志 / 工单 / 话术。程序会按你的开关，在 **🔒 接单 TT** 标签里自动操作。

常见能力：

- 自动接单（可选自动建群、自动发话术）
- 来单自动改标题 / 批量标题检测
- 按地区把 PM 拉进大象群
- 单条或批量改优先级、关键词自动升高
- 48 小时时效提醒
- 高频单站、短时批量故障告警
- 多标签浏览其它网站（不影响后台接单）

---

## 2. 安装与更新

### 安装

1. 运行 `TTDesktop1.0-Setup-1.8.2.exe`
2. 可自选安装目录
3. 安装后桌面会有快捷方式

同一台电脑只允许开一个实例。再点一次图标，会把已有窗口拉到前台。

### 自动更新

- 安装版启动约 **5 秒**后会检查 S3 上的新版本
- 发现新版本会弹窗：**立即更新 / 稍后再说**
- 下载完成后可选择立即重启安装
- 也可菜单 **帮助 → 检查更新…** 手动检查

`npm start` 开发模式不会检查更新。

### 关闭窗口

点窗口关闭 / 最小化，一般是**藏到托盘**，不会退出。要彻底退出：托盘右键退出，或菜单 **文件 → 退出**（Alt+F4）。

---

## 3. 首次配置（必做）

### 3.1 处理人 MIS

顶部 **处理人 MIS** 填本人账号，例如 `wb_lidelei`。  
必须和 TT 登录账号一致，否则「仅看我的」、接单、改标题都会对不上。

### 3.2 API 设置（强烈建议）

菜单 **文件 → API 设置…**

| 项 | 说明 |
|---|---|
| authorization | TT API 令牌，形如 `Basic xxxxx` |
| username | 本人 MIS，建议与顶部处理人一致 |
| env | 正式环境选 **prod** |
| rgIds | 本人会接到的工单组，**一行一个**，点「+ 添加工单组」 |

保存后下次拉单立即生效。配好后列表更稳、漏单更少。

未配置 API 时，程序仍可从 TT 页面抓列表，但刷新和故障扫描会慢一些。

### 3.3 登录 TT

右侧会打开 TT 待处理页。请先用公司账号登录，并保持登录态。  
没登录时，接单 / 改标题 / 拉人都无法操作页面。

---

## 4. 界面说明

### 顶部

| 控件 | 作用 |
|---|---|
| 待处理 | 当前待处理数量 |
| 距下次检查 | 自动接单开启后的倒计时 |
| 处理人 MIS | 本人账号 |
| 间隔（秒） | 自动接单检查间隔，建议 15～60，范围 3～300 |
| 自动建群 | 接单后自动建大象群 |
| 自动发送话术 | 接单后按模板发话术 |
| 标题巡检（仅日志） | 定时检查标题是否规范，**只写日志，不改标题** |
| 来单改标题 | 新工单自动改成规范标题 |
| 开 / 关 | **自动接单总开关** |

顶栏还可能出现：`48h`、`高频单站`、`短时批量` 徽章。

### 左侧三个页签

- **日志**：执行过程、成功/失败、告警
- **工单**：列表、筛选、优先级、改标题、拉 PM
- **话术**：关键词规则与默认话术

左侧可用 ◀ 收起，或 `Ctrl+\`。收起后点右侧 ▶ 展开。

### 右侧浏览器

- 第一个标签固定为 **🔒 接单 TT**，专门做接单 / 发话术 / 改标题 / 拉 PM
- `+` 可开浏览标签（最多 20 个），任意 http/https 网站
- 浏览其它站点时，后台仍可用接单 TT 操作，不必切回来盯着

---

## 5. 日常接单

### 推荐组合

| 场景 | 建议开关 |
|---|---|
| 只想自动接单 | 开「开始」，按需勾选建群 / 发话术 |
| 只想规范标题 | 只开「来单改标题」，不要开「开始」 |
| 边改标题边接单 | 「来单改标题」+「开始」一起开 |

### 操作步骤

1. 确认已登录 TT，处理人 MIS 正确
2. 需要发话术时，先到 **话术** 页配好规则并保存
3. 需要规范标题时，勾选 **来单改标题**
4. 打开顶部 **开**（自动处理）
5. 看 **日志** 是否在刷新、接单、改标题

开启后程序会按间隔检查：有待处理单则开始处理；若同时开来单改标题，会 **先改标题再接单**。

**电表异常单**：标题含【电表异常】且为 Retail / it_retail 时，仍会接单，但**不会建群、不会发话术**（避免系统告警单无群刷屏）。

### 停止

把开关拨到 **关**。  
若「来单改标题」仍勾选，停止接单后 **仍会继续自动改标题**。

---

## 6. 改标题

### 6.1 来单改标题（自动）

勾选顶部 **来单改标题** 后：

1. 程序记下当前已有工单作为基线
2. 之后新出现的未处理单（含转单）会自动改标题
3. **不弹确认**，改完写日志

发现新单的方式有两种：列表刷新 / API，以及每约 2 秒扫一次 TT 左侧列表。

工单面板里的 **来单改标题** 按钮：对当前已发现的新单立刻跑一轮。

### 6.2 标题检测（手动批量）

工单页 → **标题检测**

- 扫描本人待处理单
- 给出建议后会 **弹窗确认**，点确定才写入

适合一次性整理存量单。

### 6.3 标题巡检（只检查不改）

- 顶部勾选：定时在日志里报不规范标题
- 工单页按钮：立刻巡检一次

### 6.4 标题规则

**中文单**

`事业部简称 + 城市 + 站/仓/店 + 原问题简述`

例如：`快驴杭州某某站网络异常`

信息来源：TT 详情里的 **发起人架构**、仓库/门店字段、自定义字段 **所在城市**（有值时优先于架构城市），必要时用 API 补全。

**英文单**

`城市 + 编号 + 站点名 + 原标题`（不加事业部）

优先用自定义字段 City / Store / ID&Name，否则用架构末级（如 `Riyadh 002 Ghirnatah`）。

### 6.5 常见跳过原因

日志里出现这些，说明这次 **不会改**：

| 日志 | 含义 |
|---|---|
| 无发起人架构路径 | 详情里读不到 `公司/…/事业部/…` |
| 无法解析城市 | 所在城市和架构都匹配不到城市 |
| 标题修改失败 | 日志会带原因，如未能进入标题编辑框、写入后界面未变 |
| 无站点信息 | 没有站 / 仓 / 店 |
| 标题已是目标格式 | 不用改 |

跳过不会挡住后续接单。架构缺失的单需要人工在 TT 里改，或确认发起人架构是否完整。

---

## 7. 工单列表

### 筛选与排序

- 标题关键词搜索
- 分类：监控 / 打印 / 网络 / PDA / 其他
- **仅看我的**、**隐藏已关闭**
- 按优先级 高→低，或按创建时间 新→旧

点列表中的一条，右侧 TT 会打开对应详情。

点 **刷新** 可立刻同步列表。

### 单条改优先级

1. 选中一条工单
2. 选择优先级（非常紧急 S1 … 低 S5）
3. 点 **设置优先级**

### 批量改优先级

1. 勾选工单，或点 **全选可见**
2. 选好目标优先级
3. 点 **批量设置优先级**
4. 需要中断时点 **停止批量**

### 关键词自动升高（S3）

默认关键词：**网络卡顿、断电、没网**  
命中后升到 **高（S3）**。

- **关键词升高优先级**：立刻扫一轮当前列表
- **自动升高(S3)**：勾选后，每次刷新列表会自动扫；与拉 PM 互斥，同时进行时会排队或提示稍后再试

每轮最多处理若干条，同一单有冷却时间，避免反复改。

---

## 8. 按地区拉 PM

### 准备 PM 表

1. 点 **同步PM表**，从 S3 拉取 `PM.csv` 到本机
2. 底部应显示「PM配置：已同步…」

表格式（一人一行）：

```csv
enabled,region_key,member_name,member_mis,note
1,杭州,王标,wb_wangbiao04,
1,杭州,康成成,kangchengcheng,
```

- `enabled`：`1` 启用，`0` 停用
- `region_key`：会匹配工单架构 / 标题里的地区
- 同一地区多人：多写几行，`region_key` 相同即可
- 线上文件路径：`4000 Agent Tools/Synchronize/PM.csv`

### 拉人步骤

1. 在工单列表 **选中一条**
2. 确认该单已建大象群（或已开自动建群并完成建群）
3. 点 **按地区拉PM**
4. 看日志是否匹配到地区、是否加成员成功

正在改标题或批量升优先级时，拉 PM 会被拒绝，等完成后再点一次。

若成员可能已在群内，「添加成员」弹窗会自动关闭（取消 / 右上角关闭），日志会写明结束原因，避免卡在弹窗上。

---

## 9. 话术模板

左侧 **话术** 页。

1. 填写 **中文默认话术**、**英文默认话术**（未命中规则时的兜底）
2. **新增规则**：上行关键词（逗号/分号分隔），下行要发送的内容
3. 规则 **从上到下** 优先匹配，标题包含任一关键词即命中（忽略空格）
4. 点 **保存**
5. 可选：先选中一条工单，点 **用当前工单标题试匹配**，在日志里看会发哪条

接单且勾选 **自动发送话术** 时，会按上述规则发送。

---

## 10. 多标签浏览与书签

### 标签

- **🔒 接单 TT**：不要拿来随便逛无关网站；接单操作都走这个标签
- `+`：新建浏览标签，可打开任意 http/https
- 浏览时若触发接单 / 发话术 / 改标题 / 拉 PM，程序会自动切到接单面操作，结束后可回到浏览页

### 书签

- 点 ★ 把当前页加入书签，可选择收藏夹
- 点击书签默认开新标签；主标签不会被书签直接跳走
- 菜单 **文件 → 书签设置…** 可改成「当前标签跳转」（主标签仍除外）

### 缩放

地址栏右侧百分比：`Ctrl + 滚轮` 缩放，最小 80%；点击百分比可恢复 100%。

---

## 11. 48 小时时效

工单页勾选：

- **48h时效提醒**：处理时长满约 40 小时预警，满 48 小时超时
- **Windows通知**：同时用系统弹窗提醒

列表里每条会显示「已历时」，颜色随时长加深。顶栏也可能出现时效徽章。

右侧 TT 工单列表标题也会染色：**约 40 小时起橙色，满 48 小时红色**（便于在页面上直接扫到风险单）。

---

## 12. 高频故障 / 短时批量

这是两套不同告警，不要看成同一个功能。

| | 高频单站 | 短时批量 |
|---|---|---|
| 含义 | 同一站点同类故障反复出现 | 短时间内多个不同站点报同类障 |
| 默认 | 7 天 ≥3 次，或 30 天 ≥5 次 | 约 15 分钟内 ≥4 个站点 |
| 是否要开「开始」 | **要**，扫描当前处理人相关单 | **不要**，列表刷新就会扫 |
| 提示 | 顶栏徽章 + 日志 + 可选 Windows 通知 | 同左 |

左侧有活跃告警时，右侧 TT 列表里**相关工单标题会标红**；告警恢复后红色会去掉。

配置文件（安装后在用户数据目录，或开发目录）：

- `hf-issue.local.json`
- `burst-outbreak.local.json`

可改 `enabled`、阈值、关键词分类。示例见项目里的 `*.local.json.example`。

---

## 13. 菜单

**文件**

- 显示窗口
- 重启程序（`Ctrl+Shift+R`）
- API 设置…
- 书签设置…
- 打开 API 配置文件
- 清理缓存…（页面卡死、登录异常时可试）
- 退出

**帮助**

- 打开 TT 系统（外部浏览器）
- 检查更新…
- 开发者工具

---

## 14. 操作互斥（重要）

接单、改标题、升优先级、拉 PM 都要点同一个 TT 页面，**不能同时跑**：

| 正在做 | 再点拉 PM | 再点升优先级 / 改标题 |
|---|---|---|
| 升优先级 | 拒绝，稍后再试 | — |
| 拉 PM | — | 升优先级会跳过或排队 |
| 改标题 | 拒绝 | — |
| 自动接单本轮忙碌 | 拒绝 | 多半排队 |

日志里出现「请稍后再试」是正常保护，等当前动作结束后再点即可。

长时间运行时，程序会控制 F5 频率，避免页面连环刷新卡死。若仍卡住：先停自动处理 → 菜单重启程序 → 必要时清理缓存。

---

## 15. 日志在哪

界面日志只保留最近一部分。完整日志在本机：

`C:\Users\<你的用户名>\AppData\Roaming\TTDesktop1.0\logs\`

按天一个文件，例如 `2026-08-13.txt`。排查接单失败、更新失败时请看这里。

---

## 16. 常见问题

**Q：列表数量和 TT 页面不一致？**  
先确认 API 的 username、rgIds 是否覆盖本人工单组；点一次刷新。程序会尽量把 API 和 TT 页面列表合并去重。

**Q：开了开始但不接单？**  
检查：已登录 TT、处理人 MIS 正确、右侧是接单 TT 且页面已加载完（不是一直「加载中」）。若开来单改标题，可能正在改标题，日志会出现等待/跳过后再接。

**Q：话术没发出去？**  
确认勾选了「自动发送话术」，话术页已保存，且大象输入框能打开。浏览其它标签时程序会自动切回接单面再发。

**Q：拉不到 PM？**  
先同步 PM 表；确认工单地区能匹配 `region_key`；该单需要已有大象群；不要在升优先级过程中点拉人。

**Q：收不到自动更新？**  
必须用安装版；电脑能访问公司 S3；启动后等几秒。也可 **帮助 → 检查更新**。失败记录在日志 `[update]`。

**Q：标题全跳过「无发起人架构」？**  
TT 详情里发起人架构不完整时程序无法拼前缀，只能跳过。可人工改标题，或等架构数据齐全后再用来单改标题。

**Q：程序关不掉？**  
关闭窗口只是进托盘。用托盘退出，或 **文件 → 退出**。

---

## 17. 建议工作流（值班）

1. 打开 TTDesktop，登录 TT  
2. 核对 MIS、API（prod + 工单组）  
3. 同步 PM 表  
4. 需要发话术 → 配规则并保存  
5. 勾选「来单改标题」（建议）  
6. 按需勾选自动建群、自动发话术、48h 提醒、自动升高(S3)  
7. 打开「开始」  
8. 看日志；浏览其它系统用副标签，不要占用 🔒 接单 TT  

---

如功能有变更，以程序内实际按钮文案和当前版本号为准。当前手册对应 **TTDesktop1.0 v1.8.2**。
